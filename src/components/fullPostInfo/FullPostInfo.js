import React from 'react';

const FullPostInfo = ({value}) => {
    return (
        <div>
            {
                value.body
            }
        </div>
    );
}

export default FullPostInfo;
